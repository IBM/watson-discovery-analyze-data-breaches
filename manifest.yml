---
declared-services:
  my-discovery-service:
    label: discovery
    plan: free
applications:
- path: .
  name: watson-discovery-analyze-data-breaches
  command: npm start
  memory: 512M
  instances: 1
  disk_quota: 256M
  random-route: true
  services:
  - discovery-breach-service
