---
declared-services:
  breaches-discovery-service:
    label: discovery
    plan: free
applications:
- path: .
  name: watson-data-breaches
  command: npm start
  memory: 512M
  instances: 1
  disk_quota: 256M
  random-route: true
  services:
  - breaches-discovery-service
